import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['k.kakaocdn.net', 'scontent-ssn1-1.cdninstagram.com'], // Add your allowed domains here
  },

};

export default nextConfig;
